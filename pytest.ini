[pytest]
testpaths = tests
pythonpath = .
addopts = -q --tb=short
markers =
    local_env: needs the full development install, not just the training stack.
        Deselect with -m "not local_env" on machines that only run training
        (Kaggle, CI), where a missing dashboard package is expected rather than
        a fault.
